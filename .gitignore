node_modules
dist
.astro
.env
.env.*
!.env.example
.DS_Store
.dev.vars

# Raw photo originals — keep locally, don't ship to git/cloudflare.
# Only the optimized derivatives (hero.jpg, work-*.jpg, etc.) get committed.
public/images/_originals/
